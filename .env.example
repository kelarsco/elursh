# Copy to .env and set values. Never commit .env to Git.

# ——— API ———
# Run "npm run dev" to start both API (3001) and Vite (8080). Vite proxies /api to the API.
API_PORT=3001
API_BASE_URL=http://localhost:3001
# VITE_API_URL: When frontend is on Vercel and backend on Railway/Render, set to backend URL (e.g. https://elursh-api.railway.app).
# Frontend will call this for /api/services, /api/send-verification-code, etc. Leave empty for same-origin (dev or full-stack deploy).
# VITE_API_URL=https://your-backend.railway.app
NODE_ENV=development
FRONTEND_ORIGIN=http://localhost:8080
# SITE_URL (or FRONTEND_ORIGIN) used for Fix-It Manual email links; production: https://elursh.com
# SITE_URL=https://elursh.com

# ——— SEO (optional; for future dynamic meta / sitemap) ———
# Public site URL used in canonical, Open Graph, and schema. Update index.html if not using dynamic meta.
# VITE_SITE_URL=https://elursh.com

# ——— Database (use DATABASE_URL or individual vars for cloud Postgres) ———
# If your password has @ or special chars, set DB_PASSWORD separately (it overrides URL):
#   DATABASE_URL=postgresql://user@localhost:5432/elursh
#   DB_PASSWORD=your-actual-password
DATABASE_URL=postgresql://user:password@localhost:5432/elursh
# DB_PASSWORD=password
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=elursh
# DB_USER=user

# ——— Sync local data to Neon (only for scripts/sync-local-to-neon.js) ———
# Set when copying local DB contents to Neon. Use your local Postgres URL.
# LOCAL_DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/elursh
# DB_POOL_MAX=10
# DB_POOL_IDLE_MS=30000

# ——— JWT (v1 API auth; required for /api/v1/auth) ———
JWT_SECRET=your-long-random-jwt-secret
JWT_EXPIRES_IN=24h
JWT_ISSUER=elursh-api
SESSION_SECRET=your-long-random-session-secret

# ——— First admin user (run once: npm run migrate && node scripts/seed-admin-user.js) ———
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-secure-password
# ADMIN_DISPLAY_NAME=Admin

# ——— Manager dashboard (legacy TOTP login for /manager) ———
MANAGER_TOTP_SECRET=your-base32-secret-here
# Generate: node scripts/generate-totp-secret.js

# ——— Paystack (see docs/PAYSTACK.md) ———
# Use LIVE keys for real payments; TEST keys (sk_test_*, pk_test_*) for development
# Get keys from https://dashboard.paystack.com/#/settings/developers
PAYSTACK_SECRET_KEY=sk_live_xxxxxxxxxxxx
PAYSTACK_PUBLIC_KEY=pk_live_xxxxxxxxxxxx
# Frontend (Vite) – public key for client-side if needed
VITE_PAYSTACK_PUBLIC_KEY=pk_live_xxxxxxxxxxxx
# PAYSTACK_USD_TO_NGN_RATE=1600

# ——— Email (verification codes, manager send-email): use Resend OR SMTP ———
# Option A: Resend (recommended; free tier: https://resend.com)
# RESEND_API_KEY=re_xxxxxxxxxxxx
# RESEND_FROM=onboarding@resend.dev
# For production: verify a domain in Resend and set RESEND_FROM=notifications@yourdomain.com

# Option B: Gmail / SMTP (use App Password: Google Account → Security → App passwords)
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_APP_PASSWORD=your-16-char-app-password
# EMAIL_FROM=your-email@gmail.com
# EMAIL_FROM_NAME=Elursh
# Or Railway-style: SENDER_EMAIL=info@elursh.com, SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_USER=..., SMTP_PASS=...

# ——— Google OAuth (optional; legacy manager login) ———
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_CALLBACK_URL=
# ADMIN_EMAILS=admin@example.com
